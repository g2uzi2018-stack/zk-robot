# TI5 T170C 原始 CAN 协议与物理关节映射规范

版本：调查稿 v1.0（2026-08-21）  
目标：不依赖 ROS、不链接官方 `.so`，使用自主 C++/Linux SocketCAN 控制本机 T170C。  
范围：本体一体化关节和傲意灵巧手；底盘、相机、六维力、IMU 不属于本文的关节 CAN 协议。

## 0. 结论与可信度标记

现有材料足够实现一个自主的常规位置驱动，包括：电机发现、位置/CSP 读取、位置 CSP 控制、反馈解析、故障读取框架、软限位、平滑、共享轴仲裁和超时监控。它还不足以称为厂家完整协议，主要缺口是抱闸、通信掉线后的固件动作、所有普通查询的逐帧响应、各型号安全参数和 PT 完整切换流程。

本文使用以下标记，开发时不可忽略：

| 标记 | 含义 |
|---|---|
| **A 已确认** | 当前源码/二进制实现与实机日志相互印证，可进入第一版驱动 |
| **B 官方定义** | 官方 SDK/文档定义明确，但本机尚未逐字节抓包验证 |
| **C 旧版定义** | 只在旧头文件或旧实现中出现，可能存在固件版本差异 |
| **D 待补齐** | 现有工程无法确定，禁止按猜测实现 |

自主驱动运行时不需要官方 `.so`。官方 SDK 只作为离线取证和台架比对样本。

## 1. 本机物理 CAN 拓扑

### 1.1 最近实机扫描确认的接口分区

| Linux 接口 | 物理分区 | 实际物理电机数 | CAN ID | 证据等级 |
|---|---|---:|---|---|
| `can0` | 腰部 + 折叠/升降 | 5 | `1–5` | **A**，多次运行日志扫描确认 |
| `can1` | 头/颈部 | 3 | `30–32` | **A** |
| `can2` | 左臂 | 7 | `23–29` | **A** |
| `can3` | 右臂 | 7 | `16–22` | **A** |
| `can4` | 左傲意手 | 1 个手控制器 | `70` (`0x46`) | 配置存在，在线状态未确认 |
| `can5` | 右傲意手 | 1 个手控制器 | `60` (`0x3C`) | 配置存在，在线状态未确认 |

注意：Linux 的 `canX` 编号受适配器枚举和启动方式影响。以上是当前定制工程最近日志中的实机映射，不应永久硬编码为机器人型号的绝对属性。物理电机唯一键必须是 `(can_interface, can_id)`；启动时应只读发现并与期望拓扑比较。

日志把 `can0` 统计成“6 个电机”，是因为业务配置分别列出腰部 `[1,2]` 和腿部 `[5,4,3,2]`，ID 2 被列了两次。物理上仍只有 ID 1–5 五台电机。

### 1.2 CAN ID 到具体物理关节

#### `can0`：Fold & WAIST

| CAN ID | 官方物理关节名 | 中文含义 | 当前定制工程中的业务语义 | 编码器/减速比 | 约束 |
|---:|---|---|---|---|---|
| 1 | `WAIST_Y` | 腰部偏航 | `waist_yaw` | 双编码器，101:1 | 安全关键轴；确认抱闸前禁止通用退出路径自动失能 |
| 2 | `Fold_P3` | 第三折叠俯仰 | 同时作为 `waist_pitch` 和腿链 `SHOULDER 3_P` | 双编码器，101:1 | **同一物理轴被两个业务组共享，只允许一个目标所有者**；直接承载防坠风险 |
| 3 | `Fold_P2` | 第二折叠俯仰 | 腿链 `SHOULDER 2_P` | 双编码器，101:1 | 重力/折叠承载轴 |
| 4 | `Fold_P1` | 第一折叠俯仰 | 腿链 `SHOULDER 1_P` | 双编码器，101:1 | 重力/折叠承载轴 |
| 5 | `Fold_R` | 折叠机构滚转/外摆 | 腿链 `SHOULDER_R` | 双编码器，101:1 | 承载机构轴 |

当前定制腿部命令数组顺序是 `[ID5, ID4, ID3, ID2]`，即：

```text
leg[0] -> ID5 Fold_R  / SHOULDER_R
leg[1] -> ID4 Fold_P1 / SHOULDER 1_P
leg[2] -> ID3 Fold_P2 / SHOULDER 2_P
leg[3] -> ID2 Fold_P3 / SHOULDER 3_P（同时复用为腰部 Pitch）
```

#### `can1`：头/颈部

| CAN ID | 官方物理关节名 | 中文含义 | 双编码器 | 减速比 |
|---:|---|---|---|---:|
| 30 | `NECK_Y` | 头部偏航 | 是 | 101:1 |
| 31 | `NECK_P` | 头部俯仰 | 是 | 101:1 |
| 32 | `NECK_R` | 头部横滚 | 是 | 101:1 |

当前定制话题使用 R/P/Y 三分量，电机取分量顺序为 `[2,0,1]`，即 ID 30 取 Yaw、ID 31 取 Roll 槽、ID 32 取 Pitch 槽。这个业务槽映射不改变官方物理关节名；接入其他上层控制器时必须显式建立语义坐标转换。

#### `can2`：左臂

| CAN ID | 物理关节名 | 中文含义 | 双编码器 | 减速比 | 当前软件限位（电机角） |
|---:|---|---|---|---:|---:|
| 23 | `L_SHOULDER_P` | 左肩俯仰 | 是 | 101:1 | `[-160°, 50°]` |
| 24 | `L_SHOULDER_R` | 左肩横滚 | 是 | 101:1 | `[-80°, 80°]` |
| 25 | `L_SHOULDER_Y` | 左肩偏航 | 是 | 101:1 | `[-150°, 150°]` |
| 26 | `L_ELBOW_Y` | 左肘旋转 | 是 | 101:1 | `[-130°, 35°]` |
| 27 | `L_WRIST_P` | 左腕俯仰 | 是 | 101:1 | `[-150°, 150°]` |
| 28 | `L_WRIST_Y` | 左腕偏航 | 是 | 101:1 | `[-40°, 25°]` |
| 29 | `L_WRIST_R` | 左腕横滚 | 是 | 101:1 | `[-130°, 33°]` |

#### `can3`：右臂

| CAN ID | 物理关节名 | 中文含义 | 双编码器 | 减速比 | 当前软件限位（电机角） |
|---:|---|---|---|---:|---:|
| 16 | `R_SHOULDER_P` | 右肩俯仰 | 是 | 101:1 | `[-50°, 160°]` |
| 17 | `R_SHOULDER_R` | 右肩横滚 | 是 | 101:1 | `[-80°, 80°]` |
| 18 | `R_SHOULDER_Y` | 右肩偏航 | 是 | 101:1 | `[-150°, 150°]` |
| 19 | `R_ELBOW_Y` | 右肘旋转 | 是 | 101:1 | `[-35°, 130°]` |
| 20 | `R_WRIST_P` | 右腕俯仰 | 是 | 101:1 | `[-150°, 150°]` |
| 21 | `R_WRIST_Y` | 右腕偏航 | 是 | 101:1 | `[-25°, 40°]` |
| 22 | `R_WRIST_R` | 右腕横滚 | 是 | 101:1 | `[-33°, 130°]` |

#### 腰部、折叠和头部当前软件限位

| CAN ID | 当前业务限位 | 说明 |
|---:|---:|---|
| 1 | `[-90°, 90°]` | 腰部 Yaw |
| 2 | 腰部视角 `[-90°,90°]`；腿链视角 `[-80°,80°]` | 两套配置冲突时必须由当前模式的唯一所有者决定，不可同时钳位后分别发送 |
| 3 | `[-158°, 0°]` | Fold_P2 |
| 4 | `[-90°, 90°]` | Fold_P1 |
| 5 | `[-10°, 10°]` | Fold_R |
| 30–32 | 各 `[-30°, 30°]` | 还需确认各电机方向和装配零偏 |

这些是当前 `retargeting_T170C.yaml` 的主机软件限位，不等于固件限位、机械硬限位或无碰撞工作空间。

### 1.3 执行器型号信息的边界

官方 T170C 文档只给出了分组型号，没有给出每个 ID 的精确型号分配：

- 头部：`CRA-RI30-40-PRO-101-2E-EC2`、`CRA-RI40-52-PRO-101-2E-EC1`
- 手臂：RI30-40、RI40-52、RI50-60、RI60-70 系列，均为 101、双编码器变体
- 腰部：`CRA-RI70-80-PRO-101-2E-EC-B2`
- 折叠：`CRA-RI110-145-PRO-2-101-B1`、`CRA-RI100-120-PRO-101-2E-EC-B2`

因此编码器类型和减速比可以按官方 YAML 建立初始配置，但每个 ID 的精确执行器型号、电流/转矩上限和抱闸类型仍需在线查询或铭牌确认。

## 2. 传输层协议

| 项目 | 规定 |
|---|---|
| 总线 | Classic CAN 2.0，非 CAN FD |
| 波特率 | `1,000,000 bit/s` |
| 帧 ID | 标准 11-bit ID |
| 请求 CAN ID | 目标电机 `motor_id` |
| 响应 CAN ID | 同一 `motor_id` |
| 普通多字节字段 | little-endian |
| 应用层校验 | 本体关节帧无额外 CRC/BCC，依赖 CAN 链路层 CRC |
| Socket API | Linux `PF_CAN / SOCK_RAW / CAN_RAW` |

推荐的 SocketCAN 接口设置：

```bash
ip link set can0 down
ip link set can0 type can bitrate 1000000 restart-ms 100
ip link set can0 up
```

驱动不得假定 `write()` 成功等于电机已执行。必须用对应 CAN ID 的新反馈、接收时间戳和错误状态确认链路仍然有效。

### 2.1 现有工程是否自动拉起 CAN

几套代码的行为不同：

| 工程 | 是否自动拉起 | 实际命令/范围 |
|---|---|---|
| 原厂 `Ti5ROBOT_PACKAGE` 的 `joint_manager` / `Ti5RobotControl.so` | **是** | SocketCAN 连接时先枚举接口；对要打开的通道调用 `can_do_stop()`，等待约 100 ms，调用 `can_set_bitrate()`，再调用 `can_do_start()`，然后才执行节点发现。不是通过 shell 调用 `ip`，而是链接 `libsocketcan` 通过 rtnetlink 配置内核接口 |
| 当前 `ti5_follow_arm` 本体控制器 | **是** | 检查 `can0`～`can12`；接口存在但未 UP 时执行 `sudo ip link set canX up type can bitrate 1000000`，然后等待约 500 ms。后续电机扫描列表实际是 `can0`～`can11` |
| 当前傲意手节点 | **否** | YAML 中 `can_auto_bringup: false`。若改为 true，则先执行 `ip link set canX down`，再执行 `ip link set canX up type can bitrate 1000000` |
| 搬箱 `robot_hw_demo` | **是** | `init_socketcan()` 初始化 `can0`～`can5`，每口执行等价于 `sudo ip link set canX down && sudo ip link set canX up type can bitrate 1000000` |
| 搬箱 `build/init_all_can.sh` | 不是最终启动 | 对 `can0`～`can7` 先以 1 Mbps 拉起、随后又置 DOWN；目的是留下“干净”的 DOWN 状态供程序重配 |

#### 2.1.1 原厂 T170C 的准确启动链

官方 T170C YAML 中 `type: 1` 表示 SocketCAN，`baud_rate: 1000` 的单位是 kbit/s。官方库在打开设备时再乘以 1000，因此最终配置是 `1,000,000 bit/s`，不是 1000 bit/s。

原厂库中 `SocketCanInterface::initCanInterface(channel, bitrate_bps)` 的等效顺序是：

```text
can_do_stop(canX)
sleep(约 100 ms)
can_set_bitrate(canX, 1000000)
can_do_start(canX)
```

对应的命令行语义为：

```bash
ip link set canX down
ip link set canX type can bitrate 1000000
ip link set canX up
```

之后 `Motor_CanGetNodeInfo()` 才开始节点发现。SocketCAN 模式参数为：最多 6 个 CAN 通道、T170C 配置中的最大应用 ID、`baud_rate=1000 kbit/s`、单次发现超时约 20 ms、设备类型 5（SocketCAN）。发现结果再按应答电机 ID 匹配到实际通道，不能把 YAML 的 `device_index/can_index` 直接等同于本机固定的 Linux `canX` 编号。

原厂首次初始化路径没有显式配置 sample point、SJW、`txqueuelen` 或 `restart-ms`；`resetDevice()` 恢复路径会调用 `can_set_restart_ms(canX, 100)`。因此 `restart-ms=100` 可以作为与原厂恢复行为一致的自主驱动策略，但不能声称它是首次启动的必需协议参数。

这一动作需要网络管理权限。原厂 `run_ti5_services.sh` 会给 `joint_manager` 设置：

```text
cap_net_admin,cap_net_raw+eip
```

实机上的官方 `joint_manager` 当前也确实具有这两个 file capabilities，所以普通用户启动时仍能在进程内把 DOWN 的 CAN 接口配置为 UP。若自主程序不使用 `sudo`，同样必须以 root 运行或为可执行文件赋予相应 capability。

官方 T170C 关节连接配置全部是 SocketCAN、1 Mbps、双编码器、减速比 101；逻辑设备分组为：左臂 ID 23–29=`device_index 0`，右臂 ID 16–22=`device_index 1`，腰/折叠/头 ID 1–5、30–32=`device_index 2`。这是软件逻辑分组；最近实机发现的 Linux 接口仍是 `can0:1–5`、`can1:30–32`、`can2:23–29`、`can3:16–22`。

当前本体控制器只显式指定 `bitrate=1000000`，没有在命令中指定 `restart-ms`、sample point、SJW 或 `txqueuelen`。2026-08-22 在线读取到 `can0`～`can3` 的内核状态为：

```text
bitrate     1000000
sample-point 0.750
restart-ms  100
txqueuelen  10
state       ERROR-ACTIVE
```

其中只有 1 Mbps 可以确定来自程序命令；其余值属于当前接口已有/驱动计算的状态，不能说是 `ti5_follow_arm` 显式配置。程序内部使用 `sudo` 的方案要求免密 sudo 或已有认证；傲意手的无 `sudo` 自动拉起则要求进程拥有 root/CAP_NET_ADMIN。

自主驱动若希望启动结果完全确定，建议由 systemd/启动脚本统一配置，而不是在实时进程中调用 `system("sudo ip ...")`：

```bash
sudo ip link set can0 down
sudo ip link set can0 type can bitrate 1000000 restart-ms 100
sudo ip link set can0 txqueuelen 10
sudo ip link set can0 up
```

对实际使用的 `can1`～`can3` 同样配置；不控制 Fold/WAIST 时可以不打开 `can0` 的业务发送路径。`restart-ms=100` 和队列长度 10 是当前实机状态，不是厂家协议硬性要求，最终仍应纳入 bus-off/延迟测试。

## 3. 数值单位与换算

### 3.1 位置

官方 T170C YAML 把 ID 1–5、16–32 全部配置为双编码器。双编码器的 CAN 位置是输出端 18-bit 多圈计数：

```text
counts_per_output_rev = 262144 = 4 × 65536
raw_position = trunc(output_rad × 262144 / (2π))
output_rad   = raw_position × 2π / 262144
```

旧定制代码中的 `4 × 65536` 是双编码器输出端一圈计数，不是减速比 4。官方配置中的真实减速比是 101:1。

若以后接入单编码器型号，位置是电机端 16-bit/圈计数：

```text
raw_position = output_rad × gear_ratio × 65536 / (2π)
output_rad   = raw_position × 2π / (gear_ratio × 65536)
```

编码器模式必须作为每个物理电机的配置字段，不能根据 CAN ID 猜测。

### 3.2 速度、电流、加速度与其他量

| 量 | CAN 原始单位 | 输出侧换算/说明 | 可信度 |
|---|---|---|---|
| 电流 | mA | `A = raw / 1000.0` | **B**，官方 API/文档确认 |
| 速度 | `0.01 Hz`，电机轴 | `output_rad_s = raw × 2π / (100 × gear_ratio)` | **A/B** |
| 加速度 | `0.01 Hz/ms`，电机轴 | 需再除以减速比换到输出端 | **B** |
| 位置 | count | 双编码器按 262144 count/输出圈 | **A/B** |
| 电池电压 | `0.01 V` | `V = raw/100` | **B** |
| 母线电压 | V | 官方 API 对外直接给 V；raw 字段宽度仍需抓包 | **B/D** |
| 电机/PCB 温度 | °C | 普通查询 raw 字段宽度仍需抓包 | **B/D** |

当前 CSV 曾出现最大/最小电流 `±13487 mA`、绝对保护值 `17983 mA`，但它只覆盖少量 ID，且同一电机的 PID 和位置字段在不同导出中明显冲突。不得把这些值当成全机关节业务限流。

## 4. 普通关节 CAN 帧

### 4.1 普通写入帧

绝大多数设置命令使用：

```text
CAN_ID = motor_id
DLC    = 5
DATA   = CMD V0 V1 V2 V3
VALUE  = int32_le(V0,V1,V2,V3)
```

查询/简单动作一般使用 `DLC=1, DATA=[CMD]`。PT 模式例外，见第 7 节。

### 4.2 `0x08` 读取当前位置 — **A 已确认**

请求：

```text
CAN_ID=motor_id  DLC=1  DATA=08
```

响应：

```text
CAN_ID=motor_id  DLC=5
DATA=08 P0 P1 P2 P3
position_raw=int32_le(P0..P3)
```

当前解析器也接受 DLC 5–7，但自主驱动应把标准响应定义为 5 字节，并对非标准长度记录原始帧。

### 4.3 `0x41` 查询 CSP — **A 已确认**

请求：

```text
CAN_ID=motor_id  DLC=1  DATA=41
```

响应没有命令回显：

```text
CAN_ID=motor_id  DLC=8
DATA=I0 I1 V0 V1 P0 P1 P2 P3
current_mA  = int16_le(I0,I1)
speed_raw   = int16_le(V0,V1)       // 0.01 Hz，电机轴
position_raw= int32_le(P0..P3)      // count
```

普通 CSP 帧没有事务号，也没有响应命令字。必须按 `(interface,id,rx_timestamp,current_mode)` 关联，不能只看 `DLC=8`。

### 4.4 `0x44` 位置 CSP — **A 已确认，第一版主控制命令**

请求：

```text
CAN_ID=motor_id  DLC=5
DATA=44 P0 P1 P2 P3
target_position=int32_le(P0..P3)
```

响应与 `0x41` 完全相同，为 8 字节 CSP，不回显 `0x44`。

对本机双编码器电机：

```cpp
int32_t raw = static_cast<int32_t>(target_rad * 262144.0 / (2.0 * M_PI));
```

### 4.5 `0x42` 电流 CSP — **B 官方定义**

```text
请求：CAN_ID=motor_id, DLC=5, DATA=42 [target_mA:int32_le]
响应：8 字节普通 CSP
```

逐电机安全电流未补齐前不得开放给普通业务。

### 4.6 `0x43` 速度 CSP — **B 官方定义**

```text
请求：CAN_ID=motor_id, DLC=5, DATA=43 [speed_raw:int32_le]
响应：8 字节普通 CSP
speed_raw = output_rad_s × gear_ratio × 100 / (2π)
```

### 4.7 非 CSP 模式命令 — **A/C**

| CMD | 请求数据 | 作用 | 可信度/约束 |
|---:|---|---|---|
| `0x01` | `DLC=1 [01]` | 伺服使能 | **B/C**；本机完整上电/抱闸联动未抓包 |
| `0x02` | `DLC=5 [02 00 00 00 00]` | 伺服去使能 | **A**；当前批量失能实现确认；Fold/WAIST 禁止无条件调用 |
| `0x0B` | `DLC=1 [0B]` | 清故障 | **A/B** |
| `0x1C` | `DLC=5 [1C current_mA:int32_le]` | 进入/保持电流模式并设置电流 | **A** 格式；缺逐轴限值 |
| `0x1D` | `DLC=5 [1D speed_raw:int32_le]` | 进入/保持速度模式并设置速度 | **A** |
| `0x1E` | `DLC=5 [1E position_raw:int32_le]` | 位置模式设置目标，不要求 CSP 回传 | **A/C**；搬箱工程实际使用 |
| `0x24` | `DLC=5 [24 max_speed_raw:int32_le]` | 最大正速度限制 | **A/B** |
| `0x25` | `DLC=5 [25 min_speed_raw:int32_le]` | 最小负速度限制 | **A/B** |

官方生成 HTML 曾把 `0x1C` 和 `0x1D` 的电流/速度含义写反。本机部署二进制、旧协议头文件和实际调用一致指向 `0x1C=电流、0x1D=速度`，本规范采用本机实现。

### 4.8 `0x59` CSP 上传/通信掉线保护 — **B 格式确认，动作待补齐**

```text
CAN_ID=motor_id
DLC=5
DATA=59 U0 U1 D0 D1
upload_period      = int16_le(U0,U1)
disconnect_protect = int16_le(D0,D1)
```

官方示例 `59 64 00 C8 00` 表示数值 100 和 200，但时间单位以及超时后是保持、速度归零、失能还是抱闸均未说明。未完成卸载台架测试前禁止自动写入。

## 5. 查询、反馈与故障

### 5.1 普通查询命令表

以下命令号来自官方/旧协议头文件。除明确列为 A 的命令外，响应通常推测为“命令回显 + 参数”，但尚未对本机所有固件逐项抓包，因此解析器必须保留 raw fallback。

| CMD | 查询内容 | 已知返回单位/说明 | 等级 |
|---:|---|---|---|
| `0x03` | 当前运行模式 | 模式枚举见 5.3 | B，raw 响应待抓 |
| `0x04` | 当前 Iq | mA | B/D |
| `0x05` | 目标 Iq | mA | B/D |
| `0x06` | 当前速度 | 0.01 Hz | B/D |
| `0x07` | 目标速度 | 0.01 Hz | B/D |
| `0x08` | 当前位置 | `[08 + int32_le]` | **A** |
| `0x09` | 目标位置 | count | B/D |
| `0x0A` | 故障状态 | 32-bit 位图 | B；逐帧布局待抓 |
| `0x10/0x11` | 速度环 Kp/Ki | 参数值 | B/D |
| `0x12/0x13` | 位置环 Kp/Kd | 参数值 | B/D |
| `0x14` | 母线电压 | V | B/D |
| `0x16/0x17` | 最大/最小加速度 | 0.01 Hz/ms | B/D |
| `0x18/0x19` | 最大/最小速度 | 0.01 Hz | B/D |
| `0x1A/0x1B` | 最大/最小位置 | count | B/D |
| `0x31/0x32` | 电机/PCB 温度 | °C | B/D |
| `0x41` | CSP | 固定 8 字节 | **A** |
| `0x70` | 编码器模式 | 旧头文件：0 多圈、1 单圈 | C/D |
| `0x71` | 8 字节控制模式 | 旧头文件：0 PT、1 轮廓位置 | C/D |
| `0x78` | 电池电压 | 0.01 V | B/D |
| `0x79` | 外圈编码器位置 | count | B/D |
| `0x85/0x86` | 电流环 Kp/Ki | 参数值 | B/D |
| `0x8F/0x93` | 电机/PCB 最高温度阈值 | °C | B/D |

官方 v2 API 还定义了“电机型号、硬件/软件版本、刹车状态、减速比”等抽象查询，但这些 API 到 raw CAN 的命令号尚未全部反解，不能自行分配命令号。

### 5.2 `0x0A` 故障位

故障值按 32-bit 位图保存；未知位必须原样保留：

| 位 | 掩码 | 含义 |
|---:|---:|---|
| 0 | `0x00000001` | 内部软件错误 |
| 1 | `0x00000002` | 过压 |
| 2 | `0x00000004` | 欠压 |
| 4 | `0x00000010` | 启动错误 |
| 5 | `0x00000020` | 速度反馈错误 |
| 6 | `0x00000040` | 过流 |
| 7 | `0x00000080` | 软件操作错误，例如运行时写 Flash |
| 16 | `0x00010000` | 编码器通信错误 |
| 17 | `0x00020000` | 电机过温 |
| 18 | `0x00040000` | PCB 过温 |
| 19 | `0x00080000` | 驱动器错误 |

`0x0B` 只用于确认故障原因并满足恢复条件后的显式清除，不应在控制循环中无条件反复发送。

### 5.3 官方运行模式枚举

| 值 | 模式 |
|---:|---|
| 0 | STOP |
| 1 | CURRENT |
| 2 | SPEED |
| 3 | POSITION |
| 4 | OUTER_POSITION |
| 5 | TORQUE |
| 6 | POSITION_FEEDFORWARD |
| 7 | POSITION_SPEED |
| 8–14 | CANopen Profile/Cyclic/Interpolation 系列 |
| 15 | CSP（`0x44` 周期位置并返回 CSP） |

模式查询 raw 帧尚未抓取。不能仅根据最后一次发送的命令在主机侧假定电机模式；至少在异常恢复和重连时重新查询或以保守状态重新建立控制。

## 6. 参数写入和管理命令

### 6.1 五字节参数命令

通用格式为 `[CMD, int32_le(value)]`，除非表中另有说明。

| CMD | 含义 | 单位/风险 |
|---:|---|---|
| `0x20/0x21` | 最大/最小电流 | mA；逐型号安全值待补齐 |
| `0x22/0x23` | 最大/最小加速度 | 0.01 Hz/ms |
| `0x24/0x25` | 最大/最小速度 | 0.01 Hz |
| `0x26/0x27` | 最大/最小位置 | count |
| `0x29/0x2A` | 速度环 Kp/Ki | 危险调参 |
| `0x2B/0x2C/0x2D` | 位置环 Kp/Ki/Kd | 不同文档对 Ki 支持不一致 |
| `0x2E` | 修改 CAN ID | 高风险，可能导致节点失联/ID 冲突 |
| `0x2F` | 固件位置限制使能 | 行为待台架验证 |
| `0x3A` | 电机电角度校准 | 高风险 |
| `0x3B/0x3C` | 轮廓加速度/jerk | 轮廓模式未完整验证 |
| `0x3F` | 修改 CAN 波特率 | 高风险，旧注释“获取”应为设置 |
| `0x46` | 梯形位置并获取 CSP | 未抓包 |
| `0x47` | 位置环前馈 | 未抓包 |
| `0x50` | 设置编码器零点 | 高风险校准 |
| `0x53` | 位置偏移/清零 | 高风险；官方命名与旧注释有差异 |
| `0x70` | 设置编码器模式 | 高风险 |
| `0x71` | 设置 8 字节控制模式 | PT/轮廓模式切换未完整验证 |
| `0x83/0x84` | 电流环 Kp/Ki | 高风险调参 |
| `0x87/0x88/0x89` | 最大/额定/最小电压 | 保护参数，禁止运动程序写入 |
| `0x8D/0x91` | 电机/PCB 最高温度 | 保护参数，禁止运动程序写入 |
| `0xD4/0xD5/0xD6/0xDA` | 校准/bootloader 相关 | 正式驱动不实现 |

### 6.2 系统与 Flash 命令

| CMD | 含义 | 约束 |
|---:|---|---|
| `0x0D` | 从 Flash 恢复参数 | 维护工具专用 |
| `0x0E` | 保存参数到 Flash | 维护工具专用，限制写入次数，电机停机时执行 |
| `0x0F` | 恢复出厂设置 | 默认编译禁用 |

上述参数命令不得进入实时运动驱动的公共 API。建议拆成单独维护程序，并要求离线清单、逐轴确认和显式危险操作令牌。

### 6.3 六字节参数族 — **C/D**

旧头文件还定义 `0x41–0x49` 为减速比、PT Kp/Kd/Kt/Kv、转矩范围和电流范围，`0x96–0x9A` 为 NTC、位置参数范围和 EtherCAT 地址。命令号与普通一/五字节命令复用，含义依赖 DLC 和内部协议版本；当前缺少可靠的六字节布局，第一版不得实现写入。

## 7. PT 力位混合模式

PT 是位置/速度误差反馈加前馈转矩的混合控制，不是普通位置模式。官方 SDK 的实时 PT 请求是没有命令字的 8 字节位打包帧，且不是 little-endian 整数字段：

```text
kp_u:      12 bit
kd_u:       9 bit
position_u:16 bit，通常映射 -12.5..+12.5 rad
speed_u:   12 bit，通常映射 -18..+18 rad/s
torque_u:  12 bit，范围由具体执行器配置

b0 = kp_u >> 7
b1 = ((kp_u & 0x7f) << 1) | ((kd_u >> 8) & 0x01)
b2 = kd_u & 0xff
b3 = position_u >> 8
b4 = position_u & 0xff
b5 = speed_u >> 4
b6 = ((speed_u & 0x0f) << 4) | ((torque_u >> 8) & 0x0f)
b7 = torque_u & 0xff
```

量化：

```text
u = round(clamp((x-min) × ((1<<bits)-1)/(max-min), 0, (1<<bits)-1))
```

观察到的反馈候选布局为：

```text
b0       状态/错误相关，准确语义待确认
b1..b2   position uint16 BE
b3,b4高  speed uint12
b4低,b5  current uint12
b6       线圈温度，候选换算 (byte-50)/2 °C
b7       PCB 温度，候选换算 (byte-50)/2 °C
```

PT 的以下信息仍缺失：完整进入/退出序列、`0x71` 真实语义、反馈 b0、不同执行器的 Kp/Kd/转矩/电流边界、掉线恢复行为。普通 CSP 和 PT 都使用同一 CAN ID、DLC=8，但布局不同，因此必须以每个电机的模式状态选择解析器，不能写成“DLC=8 就按 CSP 解析”。第一版正式驱动应禁用 PT。

## 8. 抱闸与 Fold/WAIST 约束

旧协议头文件定义：

```text
0x48, DLC=1：SET_BREAKE_OPEN，失能刹车/开闸
0x49, DLC=1：SET_BREAKE_LOCK，使能刹车/抱闸
```

这两个命令属于 **C 旧版定义**。官方 v2 系统命令枚举没有显式开闸/抱闸，只提供抽象的 `MOTOR_DEVICE_PARA_GET_BRAKE` 查询；其 raw 命令、响应 DLC、0/1 语义以及“线圈命令状态还是机械到位状态”均未反解。

当前两个工程的实际行为：

- 搬箱工程对 Fold/WAIST 使用 `0x1E` 位置命令，没有显式发送 `0x48/0x49`。
- 定制控制器未发现抱闸调用。腿部进入 hold 时排除腰部 ID `[1,2]`，实际向 `[5,4,3]` 批量发送 `0x02`。
- 搬箱工程的 SIGINT/SIGTERM 路径直接 `_exit()`，不发送退出安全帧。

这些事实可能表示驱动器固件自动管理制动器，也可能表示现场所谓“抱死”是伺服保持，不能据此作结论。协议补齐前的强制约束：

1. 生产路径不发送 `0x48/0x49`。
2. ID 1–5 默认不允许业务层自动 `0x02` 去使能。
3. ID 2–5 属于折叠承载轴；通信故障时优先停止更新目标并保持最后安全位置，不能未经验证直接失能。
4. 软件位置保持无法覆盖主机掉电，最终防坠必须由已确认的机械抱闸/STO/安全继电器承担。

## 9. 傲意灵巧手 CAN 协议

傲意手不是本体电机协议。其应用包结构为：

```text
55 AA HAND_ID 01 CMD LEN PAYLOAD... LRC
```

- `01` 为主站 ID。
- `LRC` 是从 `HAND_ID` 到最后一个 payload 字节的逐字节 XOR。
- 完整应用包按每 8 字节切成多个 Classic CAN 帧，所有分片使用 `HAND_ID` 作为 CAN ID。
- 接收端按字节流重组；看到 `55 AA` 后根据 `LEN` 判断完整包长度。
- 协议没有 ISO-TP 序号或流控，同 ID 分片不能被其他发送者插入。

主要命令：

| CMD | 含义 | payload |
|---:|---|---|
| `0x50` | 六路手指/拇指位置 | 每路 `[position_u16_le, speed_u8]`，6 路共 18 字节 |
| `0x5F` | 查询状态 | `[0x80]` |
| `0x46` | 单路电流限制 | `[finger_id, limit_u16_le]` |
| `0x47` | 单路力限制 | `[finger_id, limit_u16_le]` |

状态 payload 至少 18 字节时，当前代码按每 3 字节读取一路位置的前两字节；达到 24 字节时，最后 6 字节为六路力值。当前配置控制频率 50 Hz、查询频率 20 Hz。左右手接口/ID 尚未用本次在线日志确认，因此启动时必须独立探测，且不要把本体关节的 `0x44` 等命令发送到手控制器。

## 10. 自主 C++ 驱动的开发约束

### 10.1 控制器唯一性

SocketCAN 允许多个进程同时打开同一接口。每个物理电机同一时刻只能有一个运动目标发布者：

- 使用全系统排他锁，例如 `/run/lock/ti5-can-controller.lock`。
- 启动前检查 `/proc/net/can/rcvlist_all`、`rcvlist_fil` 和已知控制进程。
- 发现其他控制器时拒绝进入运动态；不能只依靠“ROS 没启动”。
- 监听程序可以并存，但不得在相同 CAN ID 上发送查询或运动帧，除非由同一调度器管理。

### 10.2 ID 2 的唯一所有权

ID 2 不是两个电机。驱动内部只创建一个 `Motor` 对象，由模式仲裁器决定目标来源：

```text
NONE       -> 不接受运动目标
WAIST_MODE -> 只接受 waist_pitch
FOLD_MODE  -> 只接受 leg[3] / Fold_P3
```

切换所有者时必须停止旧目标、等待反馈稳定、用当前反馈作为新控制器首目标，再允许新目标变化。禁止两个业务线程各自向 ID 2 直接写 CAN。

### 10.3 坐标层次

CAN 位置只是电机/输出编码器坐标，不自动等于 URDF 关节角：

```text
业务/模型角
 -> 语义关节映射
 -> 方向符号
 -> 装配零偏/标定基线
 -> 机械与碰撞限位
 -> 电机输出角
 -> 编码器 count
 -> CAN 帧
```

当前左右臂重定向缩放和偏置是外骨骼到机器人业务坐标的配置，不应误当成电机原点标定。自主驱动应把 `direction`、`zero_offset_rad`、`encoder_type`、`gear_ratio`、软限位分别保存，并在实机逐轴确认。

补充核查显示，官方 T170C 控制链没有逐轴软件 `scale/offset`；官方 GUI 的 22 轴历史日志也能以 `raw_count×2π/262144` 直接还原动作目标。因此 CAN raw 到厂商电机角的第一版配置可统一使用 `direction=+1, zero_offset=0`。电机零点实际由驱动器内部的位置偏移承担。另有一层只供特定 IK/MDH 模型使用的 `motor_angle <-> model_q` 映射：T7/T170 左右臂及三折叠轴存在方向和 ±π/2 类偏置，详见同目录《TI5 T170C 现有标定与坐标映射核查》。这层模型映射不得重复填入 encoder calibration。

### 10.4 接收与事务模型

- 每个接口一个发送锁；同接口串行发送，不同接口可以并行。
- 一个集中接收线程使用 `poll()` 读取所有 CAN fd；查询函数不得直接抢 `read()`。
- 用 SocketCAN filter 只接收本接口期望的电机 ID 和错误帧。
- 所有接收状态带单调时钟时间戳和递增的本地主机序号。
- 普通 8 字节 CSP 无命令回显/事务号；不能靠请求—响应同步阻塞模型关联。
- 对异常 DLC、未知命令、未知错误位保留完整原始帧并计数。

### 10.5 自动发现

启动扫描只允许发送 `0x08`：

1. 枚举存在且已 UP 的 `canX`。
2. 清理接收缓存。
3. 对每个期望 ID 逐接口发送 `[0x08]`。
4. 50 ms 内只接受相同 ID、`DLC>=5`、`data[0]==0x08` 的响应。
5. 连续确认 2–3 次才建立映射；当前原工程最大重试 5 次。
6. 同一 `(接口,ID)` 只创建一个物理电机对象。
7. 同一 ID 在多个接口响应并不天然冲突，因为接口是唯一键；但必须与预期分区核对。

自动分口、反馈缓存、平滑和限位都是主机应用功能，不是 ROS 框架或 CAN 总线自动提供的能力。

### 10.6 控制周期和总线调度

当前定制工程的参考值：

```text
控制/状态基准：100 Hz
同接口相邻电机发送间隔：50 us
一批发送后的反馈等待：200 us
发送累计失败阈值：30
```

官方测试曾使用更高频率，不代表整机生产推荐。自主驱动第一阶段使用 50–100 Hz；同一周期用 `0x44` 已获得反馈时，不再额外发送 `0x08/0x41`。连续 2–3 个周期无新反馈时停止接受新轨迹并进入故障保持。

### 10.7 必须由自主代码实现的能力

| 能力 | 电机固件/CAN 是否自动提供 | 自主驱动责任 |
|---|---|---|
| 电流/速度/位置底层闭环 | 固件提供 | 选择模式、下发目标、监控反馈 |
| CAN 仲裁、ACK、链路 CRC | CAN 控制器提供 | 监控错误帧、bus-off 与重启 |
| 位置/速度等固件保护参数 | 固件可能提供 | 读取并核对；未经验证不写入 |
| 关节软限位和碰撞限制 | 不完整 | 必须实现 |
| 轨迹插值、速度/加速度/jerk 平滑 | 不提供 | 必须实现 |
| 自动分口 | 不提供 | 固定配置或只读扫描 |
| ID 2 共享轴仲裁 | 不提供 | 必须实现 |
| 反馈新鲜度、命令超时、重连 | 不完整 | 必须实现 |
| 抱闸/STO 防坠 | 当前不明确 | 软件只管理经过验证的接口，硬件承担最终安全 |

## 11. 第一版可实现边界

### 可以直接实现

- SocketCAN 接口配置、过滤、错误帧监控和统一接收线程。
- 用 `0x08` 发现 `(interface,id)`。
- `0x08` 位置读取、`0x41` CSP 查询。
- `0x44` 双编码器位置 CSP 控制和 8 字节反馈解析。
- 逐轴软件限位、最大单周期步长、速度/加速度轨迹限制。
- ID 2 唯一所有权、反馈超时、发送失败和 bus-off 状态机。
- `0x0A` raw 故障查询框架和已知位解析；在抓到响应后锁定最终帧格式。
- 正常控制只读电机参数并生成设备清单，不自动修改参数。

### 需要实验开关

- `0x42/0x43` CSP 电流/速度控制。
- `0x1C/0x1D/0x1E` 非 CSP 模式切换。
- `0x24/0x25` 速度限制写入。
- `0x59` 通信保护。
- 固件位置限位写入。
- 抱闸动作和任何去使能流程。

### 正式驱动默认不实现

- PT 力位模式。
- PID、CAN ID、波特率、电压、温度和编码器校准写入。
- Flash 保存、恢复出厂和 bootloader。

## 12. 待补齐协议清单

### P0：影响安全或基础兼容性，必须先补

| 待补项 | 当前已知 | 缺少什么 | 推荐取得方式 |
|---|---|---|---|
| 抱闸协议 | 旧版 `0x48/0x49`；官方有抽象刹车查询 | raw 查询命令、响应 DLC、值语义、开/闭确认、断电默认、与使能联动 | 机械支撑下抓官方工具查询/动作报文，核对电气图和制动器型号 |
| Fold 去使能行为 | 现有 ROS 会失能 ID 3–5，不失能 1/2 | ID 2–5 在 `0x02`、掉电、断 CAN 时是否下落/自动抱闸 | 支撑或卸载条件下逐轴试验 |
| `0x59` | 字节布局已知 | 时间单位和超时后的确切动作 | 卸载台架计时、拔 CAN/停发测试 |
| 每轴方向和零点 | 已找到第一层 `raw->motor` 直接映射证据，以及臂/三折叠轴的第二层模型映射 | 仍需做一次 22 轴同步只读快照；ID 1、5、30–32 的动态正方向需小幅验证 | 先与官方 SDK 静态读数逐轴比对，只对未通过项做小角度方向验证，不重新写零位 |
| 每 ID 精确型号 | 只有分组型号 | 型号、序列号、固件、抱闸类型逐轴映射 | 官方型号查询 raw 抓包或铭牌清单 |
| 安全限值 | CSV 不完整且冲突 | 每型号连续/峰值电流、速度、加速度、温度、转矩和持续时间 | 厂家参数表 + 逐轴台架验证 |

### P1：完成可靠常规驱动所需

| 待补项 | 缺口 |
|---|---|
| `0x0A` raw 响应 | 确认 DLC、命令回显和 32-bit 字节位置 |
| 普通查询响应 | `0x03–0x07`、`0x09`、`0x10–0x1B`、`0x31/32`、`0x70/71/78/79/85/86/8F/93` 的逐帧格式、无效命令响应和超时 |
| 使能序列 | 上电、`0x01`、清错、首目标与抱闸之间的顺序和 ACK |
| 固件差异 | 本机各电机软件版本与 `0x1C/0x1D`、查询格式兼容矩阵 |
| ACK/错误机制 | 设置命令是否响应、保存前后生效时机、拒绝原因表示方式 |
| CAN 错误恢复 | bus-off 后驱动器/适配器行为、是否需要重新使能和重发限制 |

### P2：高级能力

| 待补项 | 缺口 |
|---|---|
| PT | 完整进入/退出、反馈 b0、Kp/Kd/Kt/转矩范围、掉线行为 |
| 轮廓/插补 | `0x1F/0x46/0x56/0x57/0x58` 的完整布局、单位和状态机 |
| 六字节参数族 | DLC=6 的真实读写方向、字段类型和版本兼容性 |
| 固件维护 | bootloader、校准、Flash 寿命和失败恢复；建议不纳入运动驱动 |
| 灵巧手 | 左右实际接口/ID、完整状态字段、错误码、分片超时和重同步行为 |

## 13. 主要证据文件

```text
# 当前定制工程
/home/ti5robot/Desktop/ti5_zhilai_ws/src/ti5_controller/config/ti5_follow_config.yaml
/home/ti5robot/Desktop/ti5_zhilai_ws/src/ti5_controller/config/retargeting_T170C.yaml
/home/ti5robot/Desktop/ti5_zhilai_ws/src/ti5_controller/include/ti5_controller/can_config.hpp
/home/ti5robot/Desktop/ti5_zhilai_ws/src/ti5_controller/Can_Driver/include/Can_Box.h
/home/ti5robot/Desktop/ti5_zhilai_ws/src/ti5_aoyihand_can/src/aoyihand_can_controller.cpp
/home/ti5robot/.ros/log/

# 官方包
/home/ti5robot/Desktop/Ti5ROBOT_PACKAGE/Ti5_ROS/TI5_ROS2_v1.0.1/ros2/src/ti5_control/install/ti5_control/share/ti5_control/config/T170C/robot_joint_config.yaml
/home/ti5robot/Desktop/Ti5ROBOT_PACKAGE/Ti5_SDK/Ti5RobotSDK_linux_arm_v2.1.0/linux_arm64/include/api/MotorCtrlApi.h
/home/ti5robot/Desktop/Ti5ROBOT_PACKAGE/Ti5_SDK/Ti5RobotSDK_linux_arm_v2.1.0/linux_arm64/runtime/Ti5RobotControl.so
/home/ti5robot/Desktop/Ti5ROBOT_PACKAGE/TI5技术开发文档/site/t170c/最新T170C开发指南.html

# 搬箱工程
/home/ti5robot/wt/release/robot_carrybox_20260630_133155/
```

## 14. 当前调查边界

本规范来自静态源码、配置、日志、官方头文件和 ARM64 库反汇编。本轮在线核查没有启动 ROS、官方 SDK 或搬箱程序，没有拉起 CAN，也没有发送任何 CAN 帧。凡标记为 D 的项目必须通过厂家文档、被动抓包或机械支撑下的受控台架实验补齐，不能依靠命令名或枚举值推断机械安全行为。
